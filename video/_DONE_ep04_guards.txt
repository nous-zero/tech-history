완료: 2026-07-30 (video-producer) — 4편 렌더 착수 전 조립기 안전장치
=====================================================================
push 안 함(총감독 일괄). 커밋: 845a170(②③) · 5ca7cf0에 혼입(④) · f10c87a(회부 2건)

[② 법무 캡션 20자 상한] 완료 — 선택: **빌드 실패(예외)**
  이유: '표시 시간 자동 연장'은 이 구조에서 실행 불가. 최상위 불변식이 "영상 길이 =
  Σwav 길이"라 컷을 늘리면 뒤 전 구간이 내레이션과 어긋나고, 캡션은 사진과 한 몸
  (Group(ph,cap))이라 캡션만 늘리면 사진 없는 자리에 표기만 뜬다.
  검산: 20자 요구 = max(2.0, 20/6=3.333, 3.5) = **3.5초**. 20자 상한만으로는 불충분
  (20자여도 3.3초 미만이면 위반) → counsel §6-3 단일 규칙 3.5초 하한을 함께 박았다.
  legal_chip() 이 유일 경로. LEGAL_CAPTION_LEGACY 에 3편 33자 1건만 사유와 함께 예외
  (같은 문구를 04편에서 쓰면 즉시 예외로 죽는 것 selftest 로 확인).

[③ 전 구간 보호영역] 완료 — 3편 실측 등록 구역 3 → **15개**, 구간 커버리지 **16/16**
  상시 구역 2(자막 대역·하단 UI 대역) + 사진 1장당 1개(켄 번즈 추종, 무대 밖이면 잠듦).
  자동 구역은 전부 enforce=False = 감사 전용 → 배치 계산에서 구조적으로 제외(화면 불변).
  커버리지 미달은 verify_output_spec 이 FAIL. 3편 차단 침범 0 · 권고 23건.
  권고/차단 분리 이유: 3편 23건 중 20건이 확립된 화면 문법(사진 위 의도한 도장·라벨).
  1~3편 권고, **4편부터 차단**(ZONE_STRICT_FROM_EP=4).

[④ 쇼츠 보호영역 이식] 완료 — Short03A 실측 등록 구역 4개 · 커버리지 5/5 · 권고 7건
  UI 가림 대역은 카메라 상대 계산(줌 추종). 하단 20%·우측 12%는 **추정치** —
  구글은 세이프존을 PNG 그림으로만 공개하고 숫자를 안 쓴다(직접 열어 확인).
  제3자 측정 하단 300~450px·우측 96~130px 중 보수적 상한 채택. 재검증 대상.
  권고 7건 중 5건이 우측 UI 대역 침범(0.07~0.66) — 이전엔 원리상 못 보던 유형.

[회부① 실사용 소재 대장] 완료 — build_v2 가 파일 여는 지점을 후킹, _render_manifest.json
  3편 실측: 실사용 9건 · **대장 미등록 5건**(별칭 문제 추정) · 미사용 14건.

[회부② 재현 표기 프레임 계수] 완료 — verify_output_spec 이 완성 mp4 를 전수 계수
  3편 실측: seg10 **5.600초**/요구 5.50 통과, 아웃트로 9.272초/요구 3.50 통과.
  ※ 조립기 타임라인 추정(5.46초)은 0.14초 과소 — 픽셀이 조립기를 반증했다.
    확정 판정은 프레임 계수로 일원화, 조립기 값은 사전 경보(여유 0.25초).

[동작 동일성 증명 — 추론 아님]
  본편: 3편 아웃트로 애니 145~152 를 1080p30 재렌더 → 파셜 8개 전건 md5 일치
        (mtime 10:32~10:34 로 실제 재렌더 확인)
  쇼츠: Short03A 를 1080x1920 30fps 통째 재렌더 → 파셜 47개 전건 md5 일치
        (mtime 10:45~10:50)
  미실시: Short03B 개별 재렌더, 01·02편 재렌더(같은 코드 경로 + selftest 의
          '자동 구역은 배치 계산에 안 낀다' 검사로 갈음)

[신규 기계 검사] video/selftest_guards.py — 렌더 없이 몇 초, 전항 통과
  실행: python video/selftest_guards.py  (종료코드 0=통과)

[다음 사람이 알아야 할 것]
  · 4편 착수 시 --layout-audit 을 먼저 돌 것(약 5분, 종료코드 2 = 불합격)
  · 4편은 자동 구역이 **차단**이다. 의도한 겹침은 claim_all_photos()/claim_bottom()
    으로 신고할 것 — 신고 안 하면 렌더 전에 막힌다
  · verify_output_spec 이 읽는 [audit] 로그 형식을 바꾸면 selftest 가 즉시 실패한다
